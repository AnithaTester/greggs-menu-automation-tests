package com.greggs.config;public class TestConfig {    private String baseUrl = "https://www.greggs.co.uk";    private String browser = "chrome";    private boolean headless = false;    private boolean mobile = false;    private int timeout = 30;    private String environment = "production";    private MobileDevice mobileDevice = MobileDevice.IPHONE_12;    // Default constructor    public TestConfig() {        System.out.println("TestConfig initialized with default values");    }    // Static method to create config from system properties    public static TestConfig fromSystemProperties() {        TestConfig config = new TestConfig();        // Read system properties and override defaults        String baseUrl = System.getProperty("baseUrl");        if (baseUrl != null && !baseUrl.isEmpty()) {            config.setBaseUrl(baseUrl);        }        String browser = System.getProperty("browser");        if (browser != null && !browser.isEmpty()) {            config.setBrowser(browser);        }        String headless = System.getProperty("headless");        if (headless != null) {            config.setHeadless(Boolean.parseBoolean(headless));        }        String mobile = System.getProperty("mobile");        if (mobile != null) {            config.setMobile(Boolean.parseBoolean(mobile));        }        String timeout = System.getProperty("timeout");        if (timeout != null) {            config.setTimeout(Integer.parseInt(timeout));        }        String environment = System.getProperty("environment");        if (environment != null && !environment.isEmpty()) {            config.setEnvironment(environment);        }        System.out.println("TestConfig created from system properties: " + config);        return config;    }    // Getters    public String getBaseUrl() {        return baseUrl;    }    public String getBrowser() {        return browser;    }    public boolean isHeadless() {        return headless;    }    public boolean isMobile() {        return mobile;    }    public int getTimeout() {        return timeout;    }    public String getEnvironment() {        return environment;    }    public MobileDevice getMobileDevice() {        return mobileDevice;    }    // Setters    public void setBaseUrl(String baseUrl) {        this.baseUrl = baseUrl;    }    public void setBrowser(String browser) {        this.browser = browser;    }    public void setHeadless(boolean headless) {        this.headless = headless;    }    public void setMobile(boolean mobile) {        this.mobile = mobile;    }    public void setTimeout(int timeout) {        this.timeout = timeout;    }    public void setEnvironment(String environment) {        this.environment = environment;    }    public void setMobileDevice(MobileDevice mobileDevice) {        this.mobileDevice = mobileDevice;    }    @Override    public String toString() {        return "TestConfig{" +                "baseUrl='" + baseUrl + '\'' +                ", browser='" + browser + '\'' +                ", headless=" + headless +                ", mobile=" + mobile +                ", timeout=" + timeout +                ", environment='" + environment + '\'' +                ", mobileDevice=" + mobileDevice +                '}';    }}